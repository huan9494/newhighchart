<%= form_for(expenditure_category, remote: true) do |f| %>
  <% if expenditure_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expenditure_category.errors.count, "error") %> prohibited this expenditure_category from being saved:</h2>

      <ul>
      <% expenditure_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<br>
  <div class="field">
    <%= f.label :name %> </br>
    <%= f.text_field :name %>
  </div>
<br>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
